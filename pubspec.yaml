name: a4g
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.0.15
  video_player: ^2.11.1
  youtube_player_iframe: ^5.2.2
  flutter_tts: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

dependency_overrides:
  matcher: ^0.12.20
  meta: ^1.18.2
  test_api: ^0.7.12
  vector_math: ^2.3.0
  video_player_avfoundation: ^2.9.6
  video_player_platform_interface: ^6.7.0
  vm_service: ^15.2.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/ailments.json
    - assets/data/questions.json
    - assets/icons/
